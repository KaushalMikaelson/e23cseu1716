{
  "name": "logging_middleware",
  "version": "1.0.0",
  "description": "Reusable logger that ships logs to the evaluation service",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "typescript": "^5.4.5"
  }
}
